<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/recent_items"
        android:icon="@drawable/ic_recent_star"
        app:showAsAction="always"
        android:title="@string/recent_items"/>

    <item android:id="@+id/history_list"
        app:showAsAction="always"
        android:icon="@drawable/ic_history"
        android:title="@string/history_list"/>

    <item android:id="@+id/contact_list"
        app:showAsAction="always"
        android:icon="@drawable/ic_group"
        android:title="@string/contact_list"/>

</menu>